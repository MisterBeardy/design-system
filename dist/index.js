// @misterbeardy/design-system 0.3.0 — generated by build.mjs, do not edit.

// components/core/Button.jsx
import { jsx } from "react/jsx-runtime";
function Button({ variant = "primary", size = "md", children, style, ...props }) {
  const variants = {
    primary: { background: "var(--accent)", color: "#fff", border: "none" },
    secondary: { background: "var(--surface)", color: "var(--text-ink)", border: "1px solid var(--border)" },
    soft: { background: "var(--accent-soft)", color: "var(--accent-text)", border: "none" },
    ghost: { background: "transparent", color: "var(--text-muted)", border: "none" }
  };
  const sizes = {
    sm: { fontSize: 13, padding: "8px 14px" },
    md: { fontSize: 14, padding: "11px 20px" }
  };
  return /* @__PURE__ */ jsx(
    "button",
    {
      ...props,
      style: {
        fontFamily: "var(--font-display)",
        fontWeight: 600,
        borderRadius: "var(--radius-md)",
        cursor: "pointer",
        ...sizes[size],
        ...variants[variant],
        ...style
      },
      children
    }
  );
}

// components/core/Card.jsx
import { jsx as jsx2 } from "react/jsx-runtime";
function Card({ children, style, ...props }) {
  return /* @__PURE__ */ jsx2(
    "div",
    {
      ...props,
      style: {
        background: "var(--surface)",
        border: "1px solid var(--border)",
        borderRadius: "var(--radius-lg)",
        padding: "var(--space-4)",
        ...style
      },
      children
    }
  );
}

// components/core/Chip.jsx
import { jsx as jsx3 } from "react/jsx-runtime";
function Chip({ tone = "neutral", mono = true, children, style, ...props }) {
  const tones = {
    neutral: { background: "var(--surface-alt)", color: "var(--text-muted)" },
    accent: { background: "var(--accent-soft)", color: "var(--accent-text)" },
    ink: { background: "var(--text-ink)", color: "var(--bg)" },
    success: { background: "var(--success-soft)", color: "var(--success-text)" },
    warning: { background: "var(--warning-soft)", color: "var(--warning-text)" },
    danger: { background: "var(--danger-soft)", color: "var(--danger-text)" }
  };
  return /* @__PURE__ */ jsx3(
    "span",
    {
      ...props,
      style: {
        display: "inline-block",
        fontFamily: mono ? "var(--font-mono)" : "var(--font-display)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: mono ? "0.04em" : "normal",
        borderRadius: "var(--radius-pill)",
        padding: "5px 12px",
        ...tones[tone],
        ...style
      },
      children
    }
  );
}

// components/core/Input.jsx
import { jsx as jsx4 } from "react/jsx-runtime";
function Input({ style, ...props }) {
  return /* @__PURE__ */ jsx4(
    "input",
    {
      ...props,
      style: {
        width: "100%",
        fontFamily: "var(--font-display)",
        fontSize: 14,
        color: "var(--text-ink)",
        background: "var(--surface)",
        border: "1px solid var(--border)",
        borderRadius: "var(--radius-md)",
        padding: "12px 14px",
        ...style
      }
    }
  );
}

// components/core/StatTile.jsx
import { jsx as jsx5, jsxs } from "react/jsx-runtime";
function StatTile({ label, value, sub, accent = false, style, ...props }) {
  return /* @__PURE__ */ jsxs(
    "div",
    {
      ...props,
      style: {
        background: "var(--surface)",
        border: "1px solid var(--border)",
        borderRadius: "var(--radius-lg)",
        padding: "12px 14px",
        ...style
      },
      children: [
        /* @__PURE__ */ jsx5("div", { style: { fontFamily: "var(--font-mono)", fontSize: 10, letterSpacing: "0.08em", textTransform: "uppercase", color: "var(--text-muted)" }, children: label }),
        /* @__PURE__ */ jsx5("div", { style: { fontFamily: "var(--font-display)", fontSize: 24, fontWeight: 700, color: accent ? "var(--accent)" : "var(--text-ink)", marginTop: 4 }, children: value }),
        sub && /* @__PURE__ */ jsx5("div", { style: { fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--text-muted)", marginTop: 2 }, children: sub })
      ]
    }
  );
}

// components/core/Group.jsx
import { jsx as jsx6, jsxs as jsxs2 } from "react/jsx-runtime";
function Group({ header, footer, children, style, ...props }) {
  return /* @__PURE__ */ jsxs2("div", { ...props, style, children: [
    header && /* @__PURE__ */ jsx6(
      "div",
      {
        style: {
          font: "var(--text-section)",
          letterSpacing: "var(--tracking-caps)",
          textTransform: "uppercase",
          color: "var(--text-muted)",
          padding: "2px var(--row-pad-x) 6px"
        },
        children: header
      }
    ),
    /* @__PURE__ */ jsx6(
      "div",
      {
        style: {
          background: "var(--surface)",
          border: "1px solid var(--border)",
          borderRadius: "var(--radius-card)",
          overflow: "hidden"
        },
        children
      }
    ),
    footer && /* @__PURE__ */ jsx6(
      "div",
      {
        style: {
          font: "var(--text-row-sub)",
          color: "var(--text-muted)",
          padding: "6px var(--row-pad-x) 0"
        },
        children: footer
      }
    )
  ] });
}

// components/core/Row.jsx
import { Fragment, jsx as jsx7, jsxs as jsxs3 } from "react/jsx-runtime";
var truncate = {
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
function Row({
  glyph,
  label,
  sub,
  value,
  trailing,
  chevron = false,
  onClick,
  disabled = false,
  style,
  ...props
}) {
  const interactive = typeof onClick === "function";
  const body = /* @__PURE__ */ jsxs3(Fragment, { children: [
    glyph,
    /* @__PURE__ */ jsxs3("span", { style: { minWidth: 0, flex: 1, textAlign: "left" }, children: [
      /* @__PURE__ */ jsx7("span", { style: { display: "block", font: "var(--text-row-label)", color: "var(--text-ink)", ...truncate }, children: label }),
      sub && /* @__PURE__ */ jsx7("span", { style: { display: "block", font: "var(--text-row-sub)", color: "var(--text-muted)", marginTop: 1, ...truncate }, children: sub })
    ] }),
    value !== void 0 && value !== null && /* @__PURE__ */ jsx7("span", { style: { flexShrink: 0, font: "var(--text-row-value)", color: "var(--text-muted)", fontVariantNumeric: "tabular-nums" }, children: value }),
    trailing,
    chevron && /* @__PURE__ */ jsx7("svg", { "aria-hidden": true, viewBox: "0 0 20 20", fill: "none", style: { width: 14, height: 14, flexShrink: 0, color: "var(--text-muted)", opacity: 0.5 }, children: /* @__PURE__ */ jsx7("path", { d: "M7.5 4.5L13 10l-5.5 5.5", stroke: "currentColor", strokeWidth: "1.75", strokeLinecap: "round", strokeLinejoin: "round" }) })
  ] });
  const shared = {
    className: "ds-row",
    "data-glyph": glyph ? "true" : void 0,
    "data-interactive": interactive && !disabled ? "true" : void 0,
    "data-disabled": disabled ? "true" : void 0,
    style
  };
  if (interactive) {
    return /* @__PURE__ */ jsx7("button", { type: "button", onClick, disabled, ...shared, ...props, children: body });
  }
  return /* @__PURE__ */ jsx7("div", { ...shared, ...props, children: body });
}

// components/core/GlyphTile.jsx
import { jsx as jsx8 } from "react/jsx-runtime";
var TONE_BG = {
  accent: "var(--accent)",
  success: "var(--success)",
  warning: "var(--warning)",
  danger: "var(--danger)",
  neutral: "var(--text-muted)"
};
function GlyphTile({ tone = "neutral", color, size, children, style, ...props }) {
  return /* @__PURE__ */ jsx8(
    "span",
    {
      "aria-hidden": true,
      ...props,
      style: {
        display: "inline-flex",
        flexShrink: 0,
        alignItems: "center",
        justifyContent: "center",
        width: size ?? "var(--glyph-size)",
        height: size ?? "var(--glyph-size)",
        borderRadius: "var(--radius-sm)",
        background: color ?? TONE_BG[tone],
        color: "#fff",
        ...style
      },
      children
    }
  );
}

// components/core/Segmented.jsx
import { jsx as jsx9, jsxs as jsxs4 } from "react/jsx-runtime";
var SIZES = {
  sm: { padding: "4px 8px", fontSize: 11 },
  md: { padding: "6px 12px", fontSize: 12 }
};
var truncate2 = { display: "block", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" };
function Segmented({ options, value, onChange, label, size = "md", style, ...props }) {
  return /* @__PURE__ */ jsx9(
    "div",
    {
      role: "radiogroup",
      "aria-label": label,
      ...props,
      style: {
        display: "flex",
        gap: 2,
        padding: 2,
        boxSizing: "border-box",
        background: "var(--surface-alt)",
        borderRadius: "var(--radius-md)",
        ...style
      },
      children: options.map((opt) => {
        const active = opt.value === value;
        return /* @__PURE__ */ jsxs4(
          "button",
          {
            type: "button",
            role: "radio",
            "aria-checked": active,
            onClick: () => onChange(opt.value),
            className: "ds-segment",
            style: {
              flex: 1,
              textAlign: "center",
              fontFamily: "var(--font-display)",
              fontWeight: 500,
              cursor: "pointer",
              // The thumb is inset inside the 2px-padded track, so its radius
              // has to come in from the track's by roughly that padding —
              // concentric, not the same number.
              borderRadius: 8,
              ...SIZES[size],
              ...active ? { background: "var(--surface)", color: "var(--text-ink)", border: "1px solid var(--border)" } : { background: "transparent", color: "var(--text-muted)", border: "1px solid transparent" }
            },
            children: [
              /* @__PURE__ */ jsx9("span", { style: truncate2, children: opt.label }),
              opt.sub && /* @__PURE__ */ jsx9("span", { style: { ...truncate2, fontSize: 10, fontWeight: 400, color: "var(--text-muted)" }, children: opt.sub })
            ]
          },
          opt.value
        );
      })
    }
  );
}

// components/core/Switch.jsx
import { jsx as jsx10, jsxs as jsxs5 } from "react/jsx-runtime";
function Switch({ checked, onChange, disabled = false, label, style, ...props }) {
  return /* @__PURE__ */ jsxs5(
    "label",
    {
      style: {
        position: "relative",
        display: "inline-flex",
        flexShrink: 0,
        alignItems: "center",
        cursor: disabled ? "default" : "pointer",
        opacity: disabled ? 0.5 : 1,
        ...style
      },
      children: [
        /* @__PURE__ */ jsx10(
          "input",
          {
            type: "checkbox",
            role: "switch",
            "aria-label": label,
            checked,
            disabled,
            onChange: (e) => onChange(e.target.checked),
            className: "ds-switch-input",
            ...props
          }
        ),
        /* @__PURE__ */ jsx10(
          "span",
          {
            "aria-hidden": true,
            className: "ds-switch-track",
            style: {
              display: "block",
              width: 37,
              height: 22,
              borderRadius: "var(--radius-pill)",
              background: checked ? "var(--success)" : "var(--border)"
            }
          }
        ),
        /* @__PURE__ */ jsx10(
          "span",
          {
            "aria-hidden": true,
            className: "ds-switch-knob",
            style: {
              position: "absolute",
              left: 2,
              width: 18,
              height: 18,
              borderRadius: "var(--radius-pill)",
              background: "#fff",
              pointerEvents: "none",
              transform: checked ? "translateX(15px)" : "translateX(0)",
              boxShadow: "0 1px 3px rgb(0 0 0 / 0.3)"
            }
          }
        )
      ]
    }
  );
}

// components/core/StatStrip.jsx
import { jsx as jsx11, jsxs as jsxs6 } from "react/jsx-runtime";
var SUB_COLOR = {
  muted: "var(--text-muted)",
  success: "var(--success)",
  warning: "var(--warning)",
  danger: "var(--danger)"
};
var truncate3 = { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" };
function StatStrip({ stats, style, ...props }) {
  return /* @__PURE__ */ jsx11("div", { ...props, style: { display: "flex", ...style }, children: stats.map((s, i) => /* @__PURE__ */ jsxs6(
    "div",
    {
      style: {
        minWidth: 0,
        flex: 1,
        // The host app may not ship a box-sizing reset; flex: 1 sizes the
        // content box, so without this the padding widens each cell past
        // its share and the strip overflows its Group.
        boxSizing: "border-box",
        padding: "10px var(--space-2)",
        textAlign: "center",
        // Divider only between cells; the enclosing Group owns the outline.
        borderLeft: i > 0 ? "var(--hairline) solid var(--border)" : void 0
      },
      children: [
        /* @__PURE__ */ jsx11(
          "div",
          {
            style: {
              font: "var(--text-stat)",
              letterSpacing: "var(--tracking-stat)",
              fontVariantNumeric: "tabular-nums",
              color: s.accent ? "var(--accent)" : "var(--text-ink)",
              ...truncate3
            },
            children: s.value
          }
        ),
        /* @__PURE__ */ jsx11(
          "div",
          {
            style: {
              font: "var(--text-stat-label)",
              letterSpacing: "var(--tracking-caps)",
              textTransform: "uppercase",
              color: "var(--text-muted)",
              marginTop: 2,
              ...truncate3
            },
            children: s.label
          }
        ),
        s.sub && /* @__PURE__ */ jsx11(
          "div",
          {
            style: {
              font: "var(--text-row-sub)",
              fontVariantNumeric: "tabular-nums",
              color: SUB_COLOR[s.subTone ?? "muted"],
              ...truncate3
            },
            children: s.sub
          }
        )
      ]
    },
    s.label
  )) });
}
export {
  Button,
  Card,
  Chip,
  GlyphTile,
  Group,
  Input,
  Row,
  Segmented,
  StatStrip,
  StatTile,
  Switch
};
